{
  "mysqlDS": {
    "host": "${RDS_HOSTNAME}",
    "port": "${RDS_PORT}",
    "name": "${RDS_DBINSTANCE}",
    "database": "${RDS_DBNAME}",
    "user": "${RDS_USERNAME}",
    "password": "${RDS_PASSWORD}",
    "connector": "mysql"
  }
}
